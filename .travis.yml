language: java
jdk:
  -openjdk11

branches:
  only:
    - master

#Travis Ci 서버의 홈
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"



before_deploy:
  - zip -r aws_spring_boot_jpa *
  - mkdir -p deploy
  - mv aws_spring_boot_jpa.zip deploy/aws_spring_boot_jpa.zip

deploy:
  - provider: s3
    access_key_id: $AWS_ACCESS_KEY # Travis repo settings에 설정된 값
    secret_access_key: $AWS_SECRET_KEY # Travis repo settings에 설정된 값
    bucket: freelec-springboot-build # S3 버킷
    region: ap-northeast-2
    acl: private # zip 파일 접근을 private으로
    local_dir: deploy # before_deploy에서 생성한 디렉토리

notifications:
  email:
    recipients:
      - dnrgnl2718@gmail.com